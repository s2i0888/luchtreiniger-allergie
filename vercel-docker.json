{
  "buildCommand": "docker run --rm -v $(pwd):/src klakegg/hugo:0.128.0-extended --gc --minify",
  "outputDirectory": "public",
  "framework": null,
  "devCommand": "docker run --rm -it -v $(pwd):/src -p 1313:1313 klakegg/hugo:0.128.0-extended server -D --bind 0.0.0.0",
  "regions": ["ams1"],
  "github": {
    "silent": true
  }
}